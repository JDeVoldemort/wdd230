const daycount = '54';
const targetEvent = Christmas;
const dayUntil = "There are " + daycount + " days until " + targetEvent + "!";

const daysUntil = `There are ${daycount} days until ${targetEvent}!`;

console.log(daysUntil);